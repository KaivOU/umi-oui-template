import React, { useState, useEffect, useCallback, useMemo, FC, useRef } from 'react';
import __Styles from './index.less';

export default () => {
    return (
        <>
            <div className={__Styles.overviewOfProductData}>overview</div>
        </>
    );
};
